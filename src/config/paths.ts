/**
 * Re-export paths: packaged defaults + user home (created on install).
 */
export {
  packageRoot,
  defaultsRoot,
  defaultsAgentsDir,
  defaultsTemplatesDir,
  defaultsConfigDir,
  ARROW_HOME,
  ENV_PATH,
  CONFIG_PATH,
  SETTINGS_PATH,
  AGENTS_YAML_PATH,
  HISTORY_PATH,
  SESSIONS_DIR,
  AGENTS_DIR,
  TEMPLATES_DIR,
  GOAL_PATH,
  PLAN_PATH,
  MEMORY_DIR,
} from "../bootstrap/paths";
